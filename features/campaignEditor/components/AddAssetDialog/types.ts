/*TODO: change these from any to actual types*/

export type AddAssetDialogTypes = {
  selectedFile: any;
  setDialogOpen: any;
  setSelectedFile: any;
  source?: "assets" | "layers";
  campaign_id: string;
};
